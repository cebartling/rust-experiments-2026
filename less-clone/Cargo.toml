[package]
name = "less-clone"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
crossterm = "0.28"
regex = "1"

[dev-dependencies]
tempfile = "3.24"
assert_cmd = "2.0"
predicates = "3.0"
